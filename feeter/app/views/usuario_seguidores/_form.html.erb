<%= form_for(@usuario_seguidor) do |f| %>
  <% if @usuario_seguidor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuario_seguidor.errors.count, "error") %> prohibited this usuario_seguidor from being saved:</h2>

      <ul>
      <% @usuario_seguidor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :seguidor_id %><br>
    <%= usuario_select :seguidor_id %>
  </div>
  <div class="field">
    <%= f.label :seguido_id %><br>
    <%= usuario_select :seguido_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
